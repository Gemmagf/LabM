# Minimal runtime deps for the deployed Streamlit portfolio app
# (Streamlit Community Cloud picks this file up automatically).
#
# For the full offline pipeline (PyMC, PySpark, tests, etc.)
# use requirements-dev.txt instead.
streamlit>=1.40
pandas>=2.0
numpy>=1.26
matplotlib>=3.7
