# Full dev dependencies: needed to run the offline pipeline scripts in
# 01_mmm_lab/ and 02_scale_lab/, plus the test suite.
# (The deployed Streamlit app only needs requirements.txt — much slimmer.)

# MMM Lab
pandas==3.0.3
requests==2.34.2
matplotlib==3.10.9
statsmodels==0.14.6
pymc==6.0.0
arviz==1.1.0
scipy==1.17.1
streamlit==1.57.0
pytest==9.0.3

# Scale Lab (Criteo / PySpark) — also needs Java 17 on the system
pyspark==4.1.1
huggingface_hub==1.15.0
hf_xet==1.5.0
pyarrow==24.0.0
